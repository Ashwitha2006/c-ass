#include<stdio.h>
main()
{	
 int n, r, rev;
 rev = 0;
 printf("enter any number");
 scanf("%d", &n);
 while(n>0)
 {
 	r = n%10;
 	n = n/10;
 	rev = r*10+r;
	 }	
	 printf("reverse of given number = %d", rev);
}
